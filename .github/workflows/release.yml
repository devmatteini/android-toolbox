name: Release

on:
  push:
    tags:
      # x.y.z version format
      - "[0-9]+.[0-9]+.[0-9]+*"

permissions:
  contents: write

jobs:
  create-release:
    if: github.repository_owner == 'devmatteini'
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # 6.0.2
        with:
          persist-credentials: false

      - name: Get the release version from the tag
        run: |
          echo "TOOLBOX_VERSION=${GITHUB_REF_NAME}" >> $GITHUB_ENV

      - name: Print release version
        run: |
          echo "Version is: $TOOLBOX_VERSION"

      - name: Create release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          ./scripts/create-github-release.sh "$TOOLBOX_VERSION"

  build:
    needs: [ create-release ]
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # 6.0.2
        with:
          persist-credentials: false

      - name: Set up Java
        uses: actions/setup-java@ad2b38190b15e4d6bdf0c97fb4fca8412226d287 # 5.3.0
        with:
          distribution: temurin
          java-version: 24.0.2

      - name: Get the release version from the tag
        run: |
          echo "TOOLBOX_VERSION=${GITHUB_REF_NAME}" >> $GITHUB_ENV

      - name: Print release version
        run: |
          echo "Version is: $TOOLBOX_VERSION"

      - name: Build release APK
        id: build_apk
        env:
          TOOLBOX_KEYSTORE_ENCODED: ${{ secrets.TOOLBOX_KEYSTORE_ENCODED }}
          TOOLBOX_KEYSTORE_PASSWORD: ${{ secrets.TOOLBOX_KEYSTORE_PASSWORD }}
          TOOLBOX_KEY_ALIAS: ${{ vars.TOOLBOX_KEY_ALIAS }}
          TOOLBOX_KEY_PASSWORD: ${{ secrets.TOOLBOX_KEY_PASSWORD }}
        run: |
          asset=$(./scripts/build-release-apk.sh "$TOOLBOX_VERSION")
          echo "APK=$asset" >> $GITHUB_OUTPUT

      - name: Upload assets
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          ./scripts/upload-github-release-assets.sh \
              "$TOOLBOX_VERSION" \
              ${{ steps.build_apk.outputs.APK }}

      - name: Delete keystore file
        if: always()
        run: rm -f toolbox-keystore.jks
